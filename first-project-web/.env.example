# 开发环境默认通过 Vite 代理访问 http://127.0.0.1:8080/api/v1
# 部署时请改为可公开访问的 HTTPS 后端地址。
VITE_API_BASE_URL=https://api.example.com/api/v1

# Markdown 图片通过上面的后端地址上传和鉴权读取，无需单独的前端 OSS 密钥。
# OSS_ENDPOINT / OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET 只填写后端 .env。
